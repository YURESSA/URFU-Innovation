version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: urfu-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./backend/app/data/innovate.db3:/app/app/data/innovate.db3
    env_file:
      - backend/app/.env

  frontend:
    build:
      context: ./frontend/frontend-vue
    container_name: urfu-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
